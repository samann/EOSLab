if [ -f  /lab/DefaultSetups/bashrc ]; then
. /lab/DefaultSetups/bashrc

fi
alias sl='ls'

function rancat(){
    wget -O catpic http://placekitten.com/$[500 + RANDOM % 500]
    lpr -P arch catpic;
}

function revtext(){
    cat "$1" | rev;
}

function there(){
    find . "$1" | perl -ne 'print $1 if m/\.([^.\/]+)$/' | sort -u;
}

alias ls='ls --color=auto'
alias grep='grep --color=auto'

